# v2.0.1 Release Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v2.0.1 with AppImage crash fix, Windows portrait loading fix, console window fix, and cross-desktop keyring support.

**Architecture:** Three files already modified (main.py, ToonTownMultiTool.spec, requirements.txt). Two remaining changes: write RELEASE_NOTES.md, set `console=False` in the spec to hide the Windows console window. Then commit, tag, and push.

**Tech Stack:** PyInstaller, GitHub Actions, appimagetool

---

### Task 1: Hide Windows console window

**Files:**
- Modify: `ToonTownMultiTool.spec:57`

- [ ] **Step 1: Change console flag**

In `ToonTownMultiTool.spec`, change `console=True` to `console=False`. This prevents Windows from spawning a visible console window alongside the GUI. On Linux this flag has no effect.

```python
    console=False,
```

- [ ] **Step 2: Verify spec is valid**

Run: `python -c "exec(open('ToonTownMultiTool.spec').read())"`
Expected: No errors

---

### Task 2: Write release notes

**Files:**
- Modify: `RELEASE_NOTES.md`

- [ ] **Step 1: Replace RELEASE_NOTES.md**

Overwrite with v2.0.1 content. Keep it brief, no emdashes (use `--` or reword). Do not explain fixes in depth. Match the tone/format of the v2.0.0 notes but shorter since this is a patch release.

Content should cover:
- Fixed: AppImage crash on Wayland (SIGSEGV when typing or pressing certain keys)
- Fixed: Toon portraits not loading in packaged builds
- Fixed: Credential storage not detecting all available keyring backends in AppImage
- Fixed: Windows EXE showing a console window
- Improved: Native Wayland support for Linux (no longer forces XCB)
- Updated: Wayland users no longer need `QT_QPA_PLATFORM=xcb` workaround

---

### Task 3: Commit, tag, and push

- [ ] **Step 1: Stage changed files**

```bash
git add main.py ToonTownMultiTool.spec requirements.txt RELEASE_NOTES.md
```

- [ ] **Step 2: Commit**

```bash
git commit -m "fix: v2.0.1 - AppImage crash, Windows portraits, console window, keyring backends"
```

- [ ] **Step 3: Tag**

```bash
git tag v2.0.1
```

- [ ] **Step 4: Push commit and tag**

```bash
git push origin main --tags
```

This triggers the GitHub Actions release workflow which builds the Windows EXE and Linux AppImage automatically.

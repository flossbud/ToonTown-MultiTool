* If all toons are present but manually disabled, and keep-alive is on, the status bar says “Service running \- no toons enabled”. This language should be adjusted. Something like, “Service running, Control Disabled, Keep-Alive key being sent to \# Toons”   
* 

* If a toon has input disabled but keep-alive on, the dot indicator should glow orange, ideally the same color as the keep-alive button and progress bar.

* If a toon has input disabled but kaap-alive off, the dot indicator should glow red.   
    
* If Toon 1 and Toon 2 are working in the app with names and portraits applied, and Toon 1 logs out without closing the game instance (so, at the menu screen to select a character), the name and portrait for Toon 2 get mistakenly applied to Toon 1 as well, so it shows duplicated. This should not be the case. The expected behavior here is that Toon 1 (or whatever window is open, without the TTR API port picking up a toon name or portrait) to revert back to Toon \#. If any toon \# window is closed out in its entirety, then the order should shift and reset by left\>right position.

* The profile circles should be different colors. They can match with the profile colors. 

* If a toon is launched on the launcher tab, and then that game is closed, it’s size and lock doesn’t revert to the same style as it would be if it was never launched. This behavior should change. 

* It might be a worthwhile idea to add a “Close” / “Quit” button when a game is launched. It can replace the Launch button instead of it showing as “Running”. 

* The dot indicator in the launcher tab for each toon should match the color of the dot indicator in the multitoon tab for the corresponding toon.

* I often find that when I go to the Keymapper tab, the Default is always expanded. I’d like it if it could remember the state of each tab and load that at next open. So if i closed the app with set 2 open and the rest closed, the next time i open the app it should show the same thing. 

* Or maybe it would be a better idea to have all labels collapsed on launch. 

* Add ‘Subtabs’ to the Logs tab. They can rest at the top of the log window, towards the top right. One tab for Input Service, one tab fo the TTR API. Another tab for raw terminal output (it should not act as a terminal \- you shouldn’t be able to type in it.) Other tabs could be added if necessary.   
* The hints toggle button is currently an i icon. This should be changed to a ? icon. 

* The i icon should still be there, but repurposed. It should show the full app build, any other information that would be good there, as well as a credit showing that I made the application \- a photo of my toon waving would be cool.

* Add animations to all tabs. 

* Add animations for switching between tabs.

* Add animation for button presses on the sidebar

* Add animation for button presses on all tabs

* Add launch animation, ToonTown MultiTool v2.0 title comes out from the right side of the blue bar that rests to the left of it

* Add a “ToonTown” theme. Bright Vibrant colors. Changed fonts. 

* Change the styling of the launcher tab. Reevaluate it at least. 

* Change the execution of the game path card. Maybe it shouldn’t even be in this tab, but instead in settings.

* Add colored borders around the toon icons. This can match their head color. 

